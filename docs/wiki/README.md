# Kestrel Guide (in-app wiki)

These pages are the **in-app Guide** (Device → Guide). Home-user English.
The web app imports them at build time (`@wiki/*.md?raw`).

Edit here, not a second copy under `system/web`. Keep pages short. Link
between pages with a slug, not a URL: `[Voice](voice)`.

Builder-level HAL / runtime docs stay in `docs/` (not this folder).
